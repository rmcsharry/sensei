{
  "name": "callGuide",
  "parameters": {
    "type": "object",
    "properties": {
      "uri": {
        "type": "string",
        "description": "The URI for the prompt endpoint of the external AI guide."
      }
    },
    "required": ["uri"]
  },
  "description": "This function allows an AI guide to call another, external AI guide that has specialized knowledge or skills, to help answer a question. The root guide will know what other guides are available to it from its internally guide registry."
}